## Goal
Refactor the frontend UI and state management in `aiforacademic.world` from a rigid 3-step Pipeline into a flexible Agentic Workspace (Canvas + Chat).

## Input
Existing React components in `src/app/app/components/` and `src/hooks/usePipeline.ts`, and the main `spec.md`.

## Output
Update `spec.md` and refactor the frontend code to implement the new Canvas architecture.

## Tech stack
Node.js, Next.js 15, React 19, TypeScript, Tailwind 4

## Constraints
Only focus on the UI/UX architecture and state management hook. Do not implement the backend LLM logic.

## Save to
/Users/admin/Documents/TUYEN_OS/02_PRODUCT/REPOS/aiforacademic.world/scripts/refactor_ui.sh
